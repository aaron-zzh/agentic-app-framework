# ============================================================
# AAF 环境变量配置示例
# 复制为 .env 并填写实际值，.env 已加入 .gitignore
# ============================================================

# ── 数据库（PostgreSQL）────────────────────────────────────
DB_HOST=localhost
DB_PORT=5432
DB_NAME=aaf-dev
DB_USERNAME=postgres
DB_PASSWORD=postgres

# ── Neo4j ─────────────────────────────────────────────────
NEO4J_URI=bolt://localhost:7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=neo4j

# ── Redis ─────────────────────────────────────────────────
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# ── JWT ───────────────────────────────────────────────────
JWT_SECRET=aaf-dev-secret-must-be-at-least-32-bytes-long!!
JWT_EXPIRE_SECONDS=7200

# ── 邮件（SMTP）──────────────────────────────────────────
MAIL_HOST=smtp.qq.com
MAIL_PORT=465
MAIL_USERNAME=your@qq.com
MAIL_PASSWORD=your-smtp-auth-code
EMAIL_FROM=noreply@xuejiai.com
# 邮件 E2E 测试收件地址（默认与 EMAIL_FROM 相同）
TEST_EMAIL=

# ── AI（OpenAI 兼容接口）─────────────────────────────────
OPENAI_API_KEY=sk-placeholder
OPENAI_BASE_URL=https://api.openai.com
OPENAI_MODEL=gpt-4o-mini

# ── AI（阿里云 DashScope / AgentScope）──────────────────
# AgentScope 示例和 DashScope 模型需要此 key
# 启用 AgentScope 示例：取消 application-dev.yaml 中 aaf.examples.agentscope.enabled 的注释
DASHSCOPE_API_KEY=
MCP_SERVER_URL=
MCP_TRANSPORT=streamable-http

# ── 文件存储 ──────────────────────────────────────────────
# STORAGE_TYPE=local | s3
STORAGE_TYPE=local
STORAGE_LOCAL_PATH=${user.home}/aaf-files

# ── 短信（可选，dev 默认不启用）─────────────────────────
# 需要短信功能时取消 application-dev.yaml 中的注释并填写以下值
SMS_ACCESS_KEY_ID=
SMS_ACCESS_KEY_SECRET=
SMS_SIGN_NAME=AAF
# 注意：签名含中文时须用 Unicode 转义，例如：SMS_SIGN_NAME=\u5b66\u8bb0AI

# ── OAuth 第三方登录（可选）──────────────────────────────
# 微信
WECHAT_APP_ID=
WECHAT_APP_SECRET=
WECHAT_REDIRECT_URI=http://localhost:3000/auth/callback/wechat

# 企业微信
WECOM_CORP_ID=
WECOM_AGENT_ID=
WECOM_SECRET=
WECOM_REDIRECT_URI=http://localhost:3000/auth/callback/wecom

# 钉钉
DINGTALK_APP_KEY=
DINGTALK_APP_SECRET=
DINGTALK_REDIRECT_URI=http://localhost:3000/auth/callback/dingtalk


# ── MCP 工具服务器（可选）────────────────────────────────────────
# 留空则 mcpToolAgent 以无工具模式运行
# 传输类型：sse | streamable-http
# 示例（NWS 天气，免费无需认证，仅覆盖美国地区）：
MCP_SERVER_URL=https://nws.caseyjhand.co
MCP_TRANSPORT=streamable-http
