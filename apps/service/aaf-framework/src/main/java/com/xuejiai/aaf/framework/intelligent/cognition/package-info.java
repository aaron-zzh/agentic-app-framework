/**
 * 五层智能架构 Layer 1 — Cognition 认知基础层。
 *
 * <p>持久级·跨 Agent 共享，为上层提供记忆、知识、价值观。
 * 记忆系统：短期(Redis) + 长期(PostgreSQL) + 图谱(Neo4j) + 程序化(Markdown)。
 */
package com.xuejiai.aaf.framework.intelligent.cognition;
