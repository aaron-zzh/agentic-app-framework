-- ============================================================
-- 支付、积分与结算 Schema
-- ============================================================

-- ==================== 积分账户 ====================

CREATE TABLE credit_account (
    id              BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    version         INTEGER      NOT NULL DEFAULT 0,
    org_id          BIGINT,
    workspace_id    BIGINT,
    user_id         BIGINT       NOT NULL,
    balance         BIGINT       NOT NULL DEFAULT 0,
    frozen          BIGINT       NOT NULL DEFAULT 0,
    total_earned    BIGINT       NOT NULL DEFAULT 0,
    total_spent     BIGINT       NOT NULL DEFAULT 0,
    owner_id        BIGINT,
    create_by       BIGINT,
    create_by_type  VARCHAR(16),
    create_time     TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_by       BIGINT,
    update_by_type  VARCHAR(16),
    update_time     TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    delete_time     TIMESTAMP(6),
    deleted         BOOLEAN      NOT NULL DEFAULT FALSE,
    remark          VARCHAR(255)
);

COMMENT ON TABLE credit_account IS '积分账户';
COMMENT ON COLUMN credit_account.balance IS '可用余额（分）';
COMMENT ON COLUMN credit_account.frozen IS '冻结金额（分）';

CREATE UNIQUE INDEX uk_credit_account_user ON credit_account(user_id) WHERE deleted = FALSE;

-- ==================== 积分流水 ====================

CREATE TABLE credit_transaction (
    id              BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    version         INTEGER      NOT NULL DEFAULT 0,
    org_id          BIGINT,
    workspace_id    BIGINT,
    account_id      BIGINT       NOT NULL,
    type            VARCHAR(20)  NOT NULL,
    amount          BIGINT       NOT NULL,
    balance_after   BIGINT       NOT NULL,
    source          VARCHAR(100),
    biz_id          VARCHAR(64),
    owner_id        BIGINT,
    create_by       BIGINT,
    create_by_type  VARCHAR(16),
    create_time     TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_by       BIGINT,
    update_by_type  VARCHAR(16),
    update_time     TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    delete_time     TIMESTAMP(6),
    deleted         BOOLEAN      NOT NULL DEFAULT FALSE,
    remark          VARCHAR(255)
);

COMMENT ON TABLE credit_transaction IS '积分流水记录';
COMMENT ON COLUMN credit_transaction.type IS 'EARN/SPEND/FREEZE/UNFREEZE';

CREATE INDEX idx_credit_transaction_account ON credit_transaction(account_id) WHERE deleted = FALSE;
CREATE INDEX idx_credit_transaction_biz ON credit_transaction(biz_id) WHERE deleted = FALSE;

-- ==================== 积分转Token规则 ====================

CREATE TABLE credit_token_rule (
    id              BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    version         INTEGER      NOT NULL DEFAULT 0,
    org_id          BIGINT,
    workspace_id    BIGINT,
    name            VARCHAR(100) NOT NULL,
    credit_amount   BIGINT       NOT NULL,
    token_amount    BIGINT       NOT NULL,
    status          VARCHAR(20)  NOT NULL DEFAULT 'ENABLED',
    priority        INTEGER      NOT NULL DEFAULT 0,
    effective_from  TIMESTAMP(6),
    effective_to    TIMESTAMP(6),
    owner_id        BIGINT,
    create_by       BIGINT,
    create_by_type  VARCHAR(16),
    create_time     TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_by       BIGINT,
    update_by_type  VARCHAR(16),
    update_time     TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    delete_time     TIMESTAMP(6),
    deleted         BOOLEAN      NOT NULL DEFAULT FALSE,
    remark          VARCHAR(255)
);

COMMENT ON TABLE credit_token_rule IS '积分转Token规则';
COMMENT ON COLUMN credit_token_rule.status IS 'ENABLED/DISABLED';
COMMENT ON COLUMN credit_token_rule.priority IS '数值越小优先级越高';

-- ==================== 支付订单 ====================

CREATE TABLE pay_order (
    id                  BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    version             INTEGER      NOT NULL DEFAULT 0,
    org_id              BIGINT,
    workspace_id        BIGINT,
    merchant_order_no   VARCHAR(64)  NOT NULL,
    subject             VARCHAR(200) NOT NULL,
    body                VARCHAR(500),
    amount              BIGINT       NOT NULL,
    status              INTEGER      NOT NULL DEFAULT 0,
    channel_code        VARCHAR(32)  NOT NULL,
    channel_order_no    VARCHAR(128),
    user_id             BIGINT       NOT NULL,
    expire_time         TIMESTAMP(6),
    success_time        TIMESTAMP(6),
    refund_amount       BIGINT       NOT NULL DEFAULT 0,
    owner_id            BIGINT,
    create_by           BIGINT,
    create_by_type      VARCHAR(16),
    create_time         TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_by           BIGINT,
    update_by_type      VARCHAR(16),
    update_time         TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    delete_time         TIMESTAMP(6),
    deleted             BOOLEAN      NOT NULL DEFAULT FALSE,
    remark              VARCHAR(255)
);

COMMENT ON TABLE pay_order IS '支付订单';
COMMENT ON COLUMN pay_order.amount IS '支付金额（分）';
COMMENT ON COLUMN pay_order.status IS '0=等待支付 10=支付成功 30=已关闭';

CREATE UNIQUE INDEX uk_pay_order_merchant_no ON pay_order(merchant_order_no) WHERE deleted = FALSE;
CREATE INDEX idx_pay_order_user ON pay_order(user_id) WHERE deleted = FALSE;

-- ==================== 业务订单 ====================

CREATE TABLE biz_order (
    id              BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    version         INTEGER      NOT NULL DEFAULT 0,
    org_id          BIGINT,
    workspace_id    BIGINT,
    order_no        VARCHAR(64)  NOT NULL,
    user_id         BIGINT       NOT NULL,
    order_type      VARCHAR(32)  NOT NULL,
    subject         VARCHAR(200) NOT NULL,
    total_amount    BIGINT       NOT NULL,
    pay_order_id    BIGINT,
    status          VARCHAR(20)  NOT NULL DEFAULT 'PENDING',
    owner_id        BIGINT,
    create_by       BIGINT,
    create_by_type  VARCHAR(16),
    create_time     TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_by       BIGINT,
    update_by_type  VARCHAR(16),
    update_time     TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    delete_time     TIMESTAMP(6),
    deleted         BOOLEAN      NOT NULL DEFAULT FALSE,
    remark          VARCHAR(255)
);

COMMENT ON TABLE biz_order IS '业务订单';
COMMENT ON COLUMN biz_order.order_type IS 'RECHARGE/PURCHASE/SUBSCRIPTION';
COMMENT ON COLUMN biz_order.status IS 'PENDING/PAID/CANCELLED/REFUNDED';
COMMENT ON COLUMN biz_order.total_amount IS '订单总金额（分）';

CREATE UNIQUE INDEX uk_biz_order_no ON biz_order(order_no) WHERE deleted = FALSE;
CREATE INDEX idx_biz_order_user ON biz_order(user_id) WHERE deleted = FALSE;
CREATE INDEX idx_biz_order_pay ON biz_order(pay_order_id) WHERE deleted = FALSE;


-- ==================== 订单明细行 ====================

CREATE TABLE biz_order_item (
    id              BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    version         INTEGER      NOT NULL DEFAULT 0,
    org_id          BIGINT,
    workspace_id    BIGINT,
    order_id        BIGINT       NOT NULL,
    product_type    VARCHAR(32)  NOT NULL,
    product_id      VARCHAR(64),
    product_name    VARCHAR(200) NOT NULL,
    quantity        INTEGER      NOT NULL DEFAULT 1,
    unit_price      BIGINT       NOT NULL,
    total_price     BIGINT       NOT NULL,
    owner_id        BIGINT,
    create_by       BIGINT,
    create_by_type  VARCHAR(16),
    create_time     TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_by       BIGINT,
    update_by_type  VARCHAR(16),
    update_time     TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    delete_time     TIMESTAMP(6),
    deleted         BOOLEAN      NOT NULL DEFAULT FALSE,
    remark          VARCHAR(255)
);

COMMENT ON TABLE biz_order_item IS '订单明细行';
COMMENT ON COLUMN biz_order_item.product_type IS 'CREDIT_PACK/TOKEN_PACK/SUBSCRIPTION/AGENT/TOOL/KNOWLEDGE';
COMMENT ON COLUMN biz_order_item.product_id IS '关联产品 ID（套餐ID/Agent ID 等）';
COMMENT ON COLUMN biz_order_item.unit_price IS '单价（分）';
COMMENT ON COLUMN biz_order_item.total_price IS '小计（分）= unit_price * quantity';

CREATE INDEX idx_biz_order_item_order ON biz_order_item(order_id) WHERE deleted = FALSE;



-- ==================== 退款单 ====================

CREATE TABLE refund_order (
    id              BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    version         INTEGER      NOT NULL DEFAULT 0,
    org_id          BIGINT,
    workspace_id    BIGINT,
    refund_no       VARCHAR(64)  NOT NULL,
    pay_order_id    BIGINT       NOT NULL,
    merchant_order_no VARCHAR(64) NOT NULL,
    channel_code    VARCHAR(32)  NOT NULL,
    refund_amount   BIGINT       NOT NULL,
    status          INTEGER      NOT NULL DEFAULT 0,
    reason          VARCHAR(500),
    channel_refund_no VARCHAR(128),
    success_time    TIMESTAMP(6),
    owner_id        BIGINT,
    create_by       BIGINT,
    create_by_type  VARCHAR(16),
    create_time     TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_by       BIGINT,
    update_by_type  VARCHAR(16),
    update_time     TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    delete_time     TIMESTAMP(6),
    deleted         BOOLEAN      NOT NULL DEFAULT FALSE,
    remark          VARCHAR(255)
);

COMMENT ON TABLE refund_order IS '退款单';
COMMENT ON COLUMN refund_order.refund_amount IS '退款金额（分）';
COMMENT ON COLUMN refund_order.status IS '0=等待退款 10=退款成功 20=退款失败';

CREATE UNIQUE INDEX uk_refund_order_no ON refund_order(refund_no) WHERE deleted = FALSE;
CREATE INDEX idx_refund_order_pay ON refund_order(pay_order_id) WHERE deleted = FALSE;

-- ==================== 对账记录 ====================

CREATE TABLE reconcile_record (
    id              BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    version         INTEGER      NOT NULL DEFAULT 0,
    org_id          BIGINT,
    workspace_id    BIGINT,
    reconcile_date  DATE         NOT NULL,
    channel_code    VARCHAR(32)  NOT NULL,
    total_count     INTEGER      NOT NULL DEFAULT 0,
    matched_count   INTEGER      NOT NULL DEFAULT 0,
    mismatch_count  INTEGER      NOT NULL DEFAULT 0,
    status          INTEGER      NOT NULL DEFAULT 0,
    diff_details    TEXT,
    total_income    BIGINT       NOT NULL DEFAULT 0,
    total_refund    BIGINT       NOT NULL DEFAULT 0,
    total_fee       BIGINT       NOT NULL DEFAULT 0,
    owner_id        BIGINT,
    create_by       BIGINT,
    create_by_type  VARCHAR(16),
    create_time     TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_by       BIGINT,
    update_by_type  VARCHAR(16),
    update_time     TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    delete_time     TIMESTAMP(6),
    deleted         BOOLEAN      NOT NULL DEFAULT FALSE,
    remark          VARCHAR(255)
);

COMMENT ON TABLE reconcile_record IS '对账记录';
COMMENT ON COLUMN reconcile_record.status IS '0=待对账 10=已对平 20=存在差异';
COMMENT ON COLUMN reconcile_record.diff_details IS '差异明细（JSON）';
COMMENT ON COLUMN reconcile_record.total_income IS '总收入（分）';
COMMENT ON COLUMN reconcile_record.total_refund IS '总退款（分）';
COMMENT ON COLUMN reconcile_record.total_fee IS '总手续费（分）';

CREATE INDEX idx_reconcile_record_date ON reconcile_record(reconcile_date, channel_code) WHERE deleted = FALSE;



-- ============================================================
-- 会员与权益 Schema（AAF-074）
-- ============================================================

-- ==================== 成长等级 ====================

CREATE TABLE level (
    id              BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    version         INTEGER      NOT NULL DEFAULT 0,
    org_id          BIGINT,
    workspace_id    BIGINT,
    code            VARCHAR(16)  NOT NULL,
    name            VARCHAR(64)  NOT NULL,
    exp_min         INTEGER      NOT NULL,
    exp_max         INTEGER      NOT NULL,
    perks           JSONB,
    sort            INTEGER      NOT NULL DEFAULT 0,
    owner_id        BIGINT,
    create_by       BIGINT,
    create_by_type  VARCHAR(16),
    create_time     TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_by       BIGINT,
    update_by_type  VARCHAR(16),
    update_time     TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    delete_time     TIMESTAMP(6),
    deleted         BOOLEAN      NOT NULL DEFAULT FALSE,
    remark          VARCHAR(255)
);

COMMENT ON TABLE level IS '成长等级（免费线）';
COMMENT ON COLUMN level.perks IS '成长小权益（JSONB）';

CREATE UNIQUE INDEX uk_level_code ON level(code) WHERE deleted = FALSE;

-- ==================== 订阅套餐 ====================

CREATE TABLE subscription_plan (
    id              BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    version         INTEGER      NOT NULL DEFAULT 0,
    org_id          BIGINT,
    workspace_id    BIGINT,
    code            VARCHAR(32)  NOT NULL,
    name            VARCHAR(64)  NOT NULL,
    duration_days   INTEGER      NOT NULL,
    price           BIGINT       NOT NULL,
    market_price    BIGINT       NOT NULL,
    status          VARCHAR(16)  NOT NULL DEFAULT 'ENABLED',
    sort            INTEGER      NOT NULL DEFAULT 0,
    owner_id        BIGINT,
    create_by       BIGINT,
    create_by_type  VARCHAR(16),
    create_time     TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_by       BIGINT,
    update_by_type  VARCHAR(16),
    update_time     TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    delete_time     TIMESTAMP(6),
    deleted         BOOLEAN      NOT NULL DEFAULT FALSE,
    remark          VARCHAR(255)
);

COMMENT ON TABLE subscription_plan IS '订阅套餐定义';
COMMENT ON COLUMN subscription_plan.duration_days IS '有效天数（0=永久）';
COMMENT ON COLUMN subscription_plan.price IS '售价（分）';

CREATE UNIQUE INDEX uk_subscription_plan_code ON subscription_plan(code) WHERE deleted = FALSE;

-- ==================== 权益定义 ====================

CREATE TABLE entitlement_def (
    id              BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    version         INTEGER      NOT NULL DEFAULT 0,
    org_id          BIGINT,
    workspace_id    BIGINT,
    code            VARCHAR(64)  NOT NULL,
    name            VARCHAR(100) NOT NULL,
    type            VARCHAR(16)  NOT NULL,
    unit            VARCHAR(32),
    description     VARCHAR(500),
    owner_id        BIGINT,
    create_by       BIGINT,
    create_by_type  VARCHAR(16),
    create_time     TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_by       BIGINT,
    update_by_type  VARCHAR(16),
    update_time     TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    delete_time     TIMESTAMP(6),
    deleted         BOOLEAN      NOT NULL DEFAULT FALSE,
    remark          VARCHAR(255)
);

COMMENT ON TABLE entitlement_def IS '权益定义字典';
COMMENT ON COLUMN entitlement_def.type IS 'BOOLEAN（开关型）/ COUNTABLE（计量型）';

CREATE UNIQUE INDEX uk_entitlement_def_code ON entitlement_def(code) WHERE deleted = FALSE;

-- ==================== 套餐×权益规则 ====================

CREATE TABLE plan_entitlement (
    id              BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    version         INTEGER      NOT NULL DEFAULT 0,
    org_id          BIGINT,
    workspace_id    BIGINT,
    plan_id         BIGINT       NOT NULL,
    ent_id          BIGINT       NOT NULL,
    quota           BIGINT       NOT NULL,
    reset_cycle     VARCHAR(16)  NOT NULL DEFAULT 'NONE',
    refill_price    BIGINT       NOT NULL DEFAULT 0,
    owner_id        BIGINT,
    create_by       BIGINT,
    create_by_type  VARCHAR(16),
    create_time     TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_by       BIGINT,
    update_by_type  VARCHAR(16),
    update_time     TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    delete_time     TIMESTAMP(6),
    deleted         BOOLEAN      NOT NULL DEFAULT FALSE,
    remark          VARCHAR(255)
);

COMMENT ON TABLE plan_entitlement IS '套餐×权益规则（多对多）';
COMMENT ON COLUMN plan_entitlement.quota IS '授予额度（-1=无限）';
COMMENT ON COLUMN plan_entitlement.reset_cycle IS 'NONE/DAILY/MONTHLY/YEARLY';
COMMENT ON COLUMN plan_entitlement.refill_price IS '额度用尽后单次充值价（积分），0=不可充值';

CREATE UNIQUE INDEX uk_plan_ent ON plan_entitlement(plan_id, ent_id) WHERE deleted = FALSE;

-- ==================== 用户订阅实例 ====================

CREATE TABLE subscription (
    id              BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    version         INTEGER      NOT NULL DEFAULT 0,
    org_id          BIGINT,
    workspace_id    BIGINT,
    user_id         BIGINT       NOT NULL,
    plan_id         BIGINT       NOT NULL,
    start_at        TIMESTAMP(6) NOT NULL,
    end_at          TIMESTAMP(6),
    status          VARCHAR(16)  NOT NULL DEFAULT 'ACTIVE',
    source_id       BIGINT,
    owner_id        BIGINT,
    create_by       BIGINT,
    create_by_type  VARCHAR(16),
    create_time     TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_by       BIGINT,
    update_by_type  VARCHAR(16),
    update_time     TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    delete_time     TIMESTAMP(6),
    deleted         BOOLEAN      NOT NULL DEFAULT FALSE,
    remark          VARCHAR(255)
);

COMMENT ON TABLE subscription IS '用户订阅实例';
COMMENT ON COLUMN subscription.status IS 'ACTIVE/EXPIRED/CANCELLED';

CREATE INDEX idx_subscription_user ON subscription(user_id, status) WHERE deleted = FALSE;

-- ==================== 订阅购买流水 ====================

CREATE TABLE subscription_record (
    id              BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    version         INTEGER      NOT NULL DEFAULT 0,
    org_id          BIGINT,
    workspace_id    BIGINT,
    user_id         BIGINT       NOT NULL,
    plan_id         BIGINT       NOT NULL,
    operation       VARCHAR(16)  NOT NULL,
    pay_order_id    BIGINT,
    pay_price       BIGINT       NOT NULL,
    pay_status      VARCHAR(16)  NOT NULL DEFAULT 'UNPAID',
    pay_time        TIMESTAMP(6),
    owner_id        BIGINT,
    create_by       BIGINT,
    create_by_type  VARCHAR(16),
    create_time     TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_by       BIGINT,
    update_by_type  VARCHAR(16),
    update_time     TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    delete_time     TIMESTAMP(6),
    deleted         BOOLEAN      NOT NULL DEFAULT FALSE,
    remark          VARCHAR(255)
);

COMMENT ON TABLE subscription_record IS '订阅购买流水';
COMMENT ON COLUMN subscription_record.operation IS 'NEW/RENEW';
COMMENT ON COLUMN subscription_record.pay_status IS 'UNPAID/PAID';

CREATE INDEX idx_subscription_record_user ON subscription_record(user_id) WHERE deleted = FALSE;

-- ==================== 用户权益额度实例 ====================

CREATE TABLE entitlement_quota (
    id              BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    version         INTEGER      NOT NULL DEFAULT 0,
    org_id          BIGINT,
    workspace_id    BIGINT,
    user_id         BIGINT       NOT NULL,
    ent_id          BIGINT       NOT NULL,
    total           BIGINT       NOT NULL,
    used            BIGINT       NOT NULL DEFAULT 0,
    remain          BIGINT       NOT NULL,
    last_reset_at   TIMESTAMP(6),
    next_reset_at   TIMESTAMP(6),
    owner_id        BIGINT,
    create_by       BIGINT,
    create_by_type  VARCHAR(16),
    create_time     TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_by       BIGINT,
    update_by_type  VARCHAR(16),
    update_time     TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    delete_time     TIMESTAMP(6),
    deleted         BOOLEAN      NOT NULL DEFAULT FALSE,
    remark          VARCHAR(255)
);

COMMENT ON TABLE entitlement_quota IS '用户权益额度实例';
COMMENT ON COLUMN entitlement_quota.total IS '本周期总额度';
COMMENT ON COLUMN entitlement_quota.remain IS '剩余额度';

CREATE UNIQUE INDEX uk_quota_user_ent ON entitlement_quota(user_id, ent_id) WHERE deleted = FALSE;
CREATE INDEX idx_quota_next_reset ON entitlement_quota(next_reset_at) WHERE deleted = FALSE AND next_reset_at IS NOT NULL;

-- ==================== 权益额度变更流水 ====================

CREATE TABLE entitlement_ledger (
    id              BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    quota_id        BIGINT       NOT NULL,
    delta           BIGINT       NOT NULL,
    operation       VARCHAR(16)  NOT NULL,
    biz_type        VARCHAR(24),
    biz_id          BIGINT,
    created_at      TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP
);

COMMENT ON TABLE entitlement_ledger IS '权益额度变更流水（账本）';
COMMENT ON COLUMN entitlement_ledger.delta IS '变化量（负数=消费，正数=充值/重置）';
COMMENT ON COLUMN entitlement_ledger.operation IS 'USE/REFILL/RESET/ADJUST';

CREATE INDEX idx_ledger_quota ON entitlement_ledger(quota_id);
CREATE INDEX idx_ledger_created ON entitlement_ledger(created_at);

-- ==================== 补 credit_account 的 exp/level_id 列（wallet 复用） ====================

ALTER TABLE credit_account ADD COLUMN IF NOT EXISTS exp INTEGER NOT NULL DEFAULT 0;
ALTER TABLE credit_account ADD COLUMN IF NOT EXISTS level_id BIGINT;

COMMENT ON COLUMN credit_account.exp IS '成长经验值';
COMMENT ON COLUMN credit_account.level_id IS '当前等级 ID';

-- ============================================================
-- 字典 Seed 数据
-- ============================================================

-- 成长等级
INSERT INTO level (code, name, exp_min, exp_max, perks, sort) VALUES
    ('L0', '普通会员', 0, 99, '{"sign_bonus": 1.0}', 0),
    ('L1', '银牌会员', 100, 499, '{"sign_bonus": 1.5}', 1),
    ('L2', '金牌会员', 500, 2147483647, '{"sign_bonus": 2.0}', 2);

-- 订阅套餐
INSERT INTO subscription_plan (code, name, duration_days, price, market_price, status, sort) VALUES
    ('FREE', '免费版', 0, 0, 0, 'ENABLED', 0),
    ('PRO', '专业版', 30, 9900, 12900, 'ENABLED', 1),
    ('TEAM', '团队版', 30, 29900, 39900, 'ENABLED', 2),
    ('ENTERPRISE', '企业版', 365, 299900, 399900, 'ENABLED', 3);

-- 权益定义
INSERT INTO entitlement_def (code, name, type, unit, description) VALUES
    ('ai_token', 'AI Token 额度', 'COUNTABLE', 'token', 'AI 对话消耗的 Token 额度'),
    ('model_gpt4', 'GPT-4 模型访问', 'BOOLEAN', NULL, '是否可使用 GPT-4 模型'),
    ('kb_storage', '知识库存储', 'COUNTABLE', 'MB', '知识库文件存储空间'),
    ('workflow_run', '工作流执行次数', 'COUNTABLE', '次', '每月可执行工作流次数');

-- 套餐×权益规则（FREE）
INSERT INTO plan_entitlement (plan_id, ent_id, quota, reset_cycle, refill_price)
SELECT sp.id, ed.id, 10000, 'MONTHLY', 100
FROM subscription_plan sp, entitlement_def ed
WHERE sp.code = 'FREE' AND ed.code = 'ai_token';

INSERT INTO plan_entitlement (plan_id, ent_id, quota, reset_cycle, refill_price)
SELECT sp.id, ed.id, 1024, 'NONE', 0
FROM subscription_plan sp, entitlement_def ed
WHERE sp.code = 'FREE' AND ed.code = 'kb_storage';

INSERT INTO plan_entitlement (plan_id, ent_id, quota, reset_cycle, refill_price)
SELECT sp.id, ed.id, 10, 'MONTHLY', 50
FROM subscription_plan sp, entitlement_def ed
WHERE sp.code = 'FREE' AND ed.code = 'workflow_run';

-- 套餐×权益规则（PRO）
INSERT INTO plan_entitlement (plan_id, ent_id, quota, reset_cycle, refill_price)
SELECT sp.id, ed.id, 1000000, 'MONTHLY', 10
FROM subscription_plan sp, entitlement_def ed
WHERE sp.code = 'PRO' AND ed.code = 'ai_token';

INSERT INTO plan_entitlement (plan_id, ent_id, quota, reset_cycle, refill_price)
SELECT sp.id, ed.id, 1, 'NONE', 0
FROM subscription_plan sp, entitlement_def ed
WHERE sp.code = 'PRO' AND ed.code = 'model_gpt4';

INSERT INTO plan_entitlement (plan_id, ent_id, quota, reset_cycle, refill_price)
SELECT sp.id, ed.id, 10240, 'NONE', 0
FROM subscription_plan sp, entitlement_def ed
WHERE sp.code = 'PRO' AND ed.code = 'kb_storage';

INSERT INTO plan_entitlement (plan_id, ent_id, quota, reset_cycle, refill_price)
SELECT sp.id, ed.id, 100, 'MONTHLY', 20
FROM subscription_plan sp, entitlement_def ed
WHERE sp.code = 'PRO' AND ed.code = 'workflow_run';
