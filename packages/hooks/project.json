{
  "name": "hooks",
  "projectType": "library",
  "sourceRoot": "packages/hooks/src",
  "targets": {
    "typecheck": {
      "command": "tsgo --noEmit -p tsconfig.lib.json",
      "options": { "cwd": "packages/hooks" }
    },
    "test": {
      "command": "vitest run",
      "options": { "cwd": "packages/hooks" }
    },
    "lint": {
      "command": "biome check packages/hooks/src"
    }
  }
}
