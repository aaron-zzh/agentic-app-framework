# AAF-114 静态代码审查

## 审查范围

- Assistant 与 Role 原子选择及 forwardedProps。
- assistant-ui 图片/文本附件到 AG-UI、执行请求和视觉解析链。
- 计划与思考展示偏好的单一状态源。
- Base UI 组合、320px 布局、语音和发送/停止回归风险。
- AAF-114 需求、设计、任务与开发记录一致性。

## 审查结果

| 轮次 | blocker | major | minor | 结果 |
|------|---------|-------|-------|------|
| 首轮 | 2 | 5 | 1 | 退回修复 |
| 复轮 | 0 | 1 | 3 | 修复可执行项，验证缺口留档 |
| 最终静态核对 | 0 | 0 | 0 | 静态通过 |

## 已闭环问题

- Role 仅与 Assistant ID 成对展示和发送；旧 roleKey 仅唯一匹配时归一。
- 图片 key 必经当前用户所有权、删除状态及 `image/*` 校验。
- 仅图片消息获得明确视觉分析指令，不再因空文本失败。
- 320px Composer 使用可收缩模型入口及图标模式开关。
- 语音识别保留录音前输入，不再整体覆盖。
- 混合剪贴板文件不被长文本 chip 拦截；附件错误提供 toast。
- `showThinking` 同时门控 runtime 摄取和已有 reasoning 渲染。
- AAF-114 编号、链接、任务状态与 dev-log 已同步。

## 剩余风险

- 用户明确要求不运行 lint、测试、typecheck、build 或 check。
- 未进行 320px 浏览器实测、真实 OSS 上传、AG-UI 请求或视觉模型端到端验证。
- Role 原子构造、仅图片消息、跨用户 key 拒绝和 MIME 拒绝尚无本轮回归测试证据。
- 因缺少自动化门禁，本记录仅代表源码静态审查通过，不代表常规质量门禁通过。
