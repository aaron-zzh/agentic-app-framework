---
level: Practice
layer: Product
purpose: AAF-071 对话生成多媒体的技术任务清单
status: done
version: 1.0.0
date: 2026-05-19
author: AaronZZH
---

# 对话生成多媒体（AAF-071）

> 负责人：architect + developer-service + developer-webui | 创建：05-19

## 任务列表

### 对话触发

1. [x] #7101 对话中生成图片
   - UI 参考：[tmp/ui/aigc-1.jpg](../../../../tmp/ui/aigc-1.jpg)（右侧对话面板交互）
   - 对话意图识别（识别"画一张…"/"生成图片…"等意图）
   - 触发图像生成服务（提取 Prompt、选择默认模型、支持@引用素材作为参考）
   - 内联预览卡片（对话消息中嵌入图片预览、点击放大）
   - 参数快捷调整（卡片上提供"换一张"/"调整风格"/"手动编辑"/"重新生成"按钮）
   - verify: 对话中输入生成指令→正确返回图片卡片

2. [x] #7102 对话中生成视频（分幕模式）
   - 对话驱动分幕视频生成（AI 根据描述自动拆分为多幕、每幕独立生成）
   - 分幕状态展示（每幕生成进度、Media Assets 完成状态、Token 消耗）
   - 素材变更同步（修改素材后提示"重新生成受影响的幕"）
   - 合成入口（"三幕素材齐全后，开始合成视频"）
   - verify: 对话触发多幕视频生成，分幕状态正确展示

### 素材联动

3. [x] #7103 一键保存素材库
   - 生成结果→一键保存按钮（保存到素材库）
   - 自动分类（根据生成类型自动归入对应分类）
   - 自动打标（基于 Prompt 内容自动生成标签）
   - 保存确认反馈（保存成功提示、跳转素材库链接）
   - verify: 一键保存后素材库中可查到，分类标签正确

4. [x] #7104 生成历史
   - 对话中生成记录列表（按时间倒序、缩略图预览）
   - 按类型筛选（图片/视频/音频）
   - 重新生成（基于历史记录参数重新生成）
   - 参数复用（选择历史记录→自动填充参数→修改后生成）
   - verify: 生成历史完整记录，参数复用功能正常

### 工具集成

5. [x] #7105 多媒体工具节点
   - 图像生成封装为 MCP 工具（工具描述、参数 Schema、返回格式）
   - 视频生成封装为 MCP 工具
   - 可在工作流编排中作为节点调用（输入 Prompt→输出素材 URL）
   - 工具调用结果自动保存到素材库
   - verify: 工作流中调用多媒体工具节点正确生成并返回结果
